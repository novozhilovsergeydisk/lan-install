<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Параметры работ, которые показываются цифрой в маркере на карте
    |--------------------------------------------------------------------------
    |
    | Привязка идёт по ID типа параметра (work_parameter_types.id), а НЕ по имени:
    | название типа заказчик периодически меняет («панели» → «Панели (новые)»),
    | и привязка по строке ломалась бы после каждого переименования.
    |
    | Сейчас в справочнике живут три «панельных» типа (исторические дубли):
    |   1  — Панели (новые)   — основной, используется с заявки #3249
    |   43 — панели           — старый, заявки #4653–4718
    |   45 — Панели, Б/У      — заявки #4540–4818
    |
    | Берётся ПЕРВАЯ по порядку запись такого параметра — это плановое количество
    | (при закрытии заявки дописывается вторая запись с фактом, и суммировать их нельзя).
    | Если у заявки нет ни одного из этих типов (напр. «Демонтаж МЭШ» — там ИП,
    | точки доступа), маркер показывает первый параметр заявки, как было раньше.
    |
    */

    'panel_parameter_type_ids' => [1, 43, 45],

];
